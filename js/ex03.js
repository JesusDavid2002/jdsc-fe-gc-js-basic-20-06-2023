// 3. Declarar una variable llamada "base" y asignarle un valor numérico que represente la base de un triángulo. 
// Luego, declarar una variable llamada "altura" y asignarle un valor numérico que represente la altura del triángulo. 
// Calcular el área del triángulo (usando la fórmula (base* altura) / 2) y mostrar el resultado en la consola.

let base = 3;
let altura = 5;

console.log('El área del triángulo es: ' + (base * altura) / 2);